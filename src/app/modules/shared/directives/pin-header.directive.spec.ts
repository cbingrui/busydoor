import { PinHeaderDirective } from './pin-header.directive';

describe('PinHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PinHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
